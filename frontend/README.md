# OrderBook Frontend

React + TypeScript + Vite + Tailwind CSS frontend for the Cross-Chain Intent Orderbook.

## Setup

```bash
npm install
npm run dev
# Open http://localhost:5173
```

## Build

```bash
npm run build
npm run preview
```

## Configuration

Edit `src/config.ts` to update contract IDs and RPC endpoints.
